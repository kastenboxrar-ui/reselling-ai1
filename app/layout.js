export const metadata = {
  title: "Reselling AI",
  description: "AI Reselling Tool"
};

export default function RootLayout({ children }) {
  return (
    <html lang="de">
      <body>{children}</body>
    </html>
  );
}
